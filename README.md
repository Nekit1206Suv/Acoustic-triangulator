# 📡 Triangulator: Geospatial Locator Dashboard

# Triangulator — это высокопроизводительное приложение для симуляции и визуализации процесса триангуляции объекта в трехмерном пространстве. Проект представляет собой инструментальную панель (Dashboard) в стиле Cyberpunk/Sci-Fi, которая в реальном времени рассчитывает координаты цели на основе данных от трех базовых станций.

# 📑 Содержание

# Обзор проекта

# Ключевые особенности

# Визуальный стиль и UI

# Логика работы

# Технический стек

# Установка и запуск

# Руководство пользователя

# Структура кода

# 🔭 Обзор проекта

# Triangulator решает классическую инженерную задачу геодезии и радиолокации: определение точного местоположения точки пересечения направленных лучей от нескольких источников. В данной реализации используется конфигурация из трех станций, образующих равносторонний треугольник, что является стандартом для обеспечения максимальной зоны покрытия и точности.

# Приложение позволяет оператору манипулировать параметрами каждой станции (угол места и азимут) и длиной стороны треугольника-основания. Любое изменение входных данных, будь то перетаскивание ползунка или ручной ввод цифр, мгновенно запускает пересчет модели, обновление цифровой статистики и перерисовку векторной карты.

# Проект был перенесен с веб-прототипа на нативный C# / WPF, что обеспечило максимальную плавность анимаций, строгую типизацию данных и возможность использования аппаратного ускорения Windows для отрисовки графики без задержек.

# ✨ Ключевые особенности

# 🚀 Реактивность и Производительность

# В отличие от стандартных форм, где требуется нажатие кнопки "Рассчитать", Triangulator работает в полностью реактивном режиме.

# Live-Binding: Ввод значений с клавиатуры мгновенно обновляет слайдеры и карту. Система распознает ввод в реальном времени, не требуя потери фокуса элементом управления.

# Синхронизация: Слайдеры и текстовые поля жестко связаны — изменение одного элемента управления немедленно отражается на другом, создавая ощущение целостного физического прибора.

# 🎯 Умная система наведения

# Мы переосмыслили систему координат для удобства пользователя, отойдя от стандартной картографии в пользу центрированной навигации:

# Азимут 0° — это направление строго в Центр треугольника (Центроид).

# Это позволяет интуитивно направлять "лазеры" всех станций в одну точку схождения, не занимаясь сложными вычислениями углов в уме.

# Положительные значения отклоняют луч по часовой стрелке, отрицательные — против часовой стрелки относительно вектора на центр.

# 📊 Детальная статистика

# Панель мониторинга предоставляет данные с высокой точностью:

# Object Height: Расчетная высота объекта над плоскостью земли (триангулированная ось Z).

# Ground X / Y: Координаты проекции точки на плоскость основания.

# Status Indicator: Индикатор состояния системы ("LOCKED" или "NO FIX"), который визуально сигнализирует о валидности текущего решения триангуляции.

# 🎨 Визуальный стиль и UI

# Дизайн приложения вдохновлен профессиональными интерфейсами из научно-фантастических фильмов и игр. Мы отказались от стандартных серых окон Windows в пользу кастомной темной стилизации для снижения нагрузки на глаза и создания атмосферы высокотехнологичного инструмента.

# Элемент	Описание стиля

# Цветовая палитра	Использованы глубокие оттенки #0B0F19 (Dark Navy) с яркими функциональными акцентами #10B981 (Emerald Green) и #3B82F6 (Electric Blue).

# Векторная графика	Карта отрисовывается программно на панели Canvas. Это гарантирует идеальную четкость линий и шрифтов на мониторах с любым разрешением (включая 4K).

# Типографика	Использование моноширинного шрифта Consolas для вывода цифровых значений придает интерфейсу технический, инженерный вид.

# Индикация	Строгое цветовое кодирование: Зеленый — станции и высота, Синий — азимутальные лучи, Красный — цель и ошибки.

# Фон карты выполнен в сплошном темном цвете #0D1117, что обеспечивает максимальный контраст для лучей наведения и маркеров станций, позволяя оператору сфокусироваться на геометрии процесса.

# 📐 Логика работы

# В основе приложения лежит файл TriangulationMath.cs, реализующий алгоритмический подход к решению геометрической задачи без использования сторонних библиотек.

# 1\. Конфигурация пространства

# Система автоматически строит равносторонний треугольник на основе введенной длины стороны.

# Первая станция помещается в начало координат.

# Вторая смещается по оси X на заданную длину.

# Третья рассчитывается геометрически, образуя вершину равностороннего треугольника.

# 2\. Расчет направлений (Азимутов)

# Для реализации удобной логики "0° = Центр", алгоритм сначала вычисляет геометрический центр (центроид) треугольника. Для каждой станции определяется базовый вектор, направленный в этот центр. Вводимый пользователем азимут считается как отклонение от этого базового вектора. Это избавляет пользователя от необходимости знать абсолютные координаты станций.

# 3\. Поиск точки пересечения

# Алгоритм решает задачу нахождения пересечения прямых линий на плоскости. Для каждой пары станций строится математическая модель прямой линии, исходящей из координат станции под заданным углом. Система находит точки, где эти линии пересекаются.

# В идеальной математической модели все три линии пересекаются в одной точке. На практике образуется микроскопический "треугольник ошибок". Итоговая координата цели на земле вычисляется как усредненный центр этого треугольника ошибок.

# 4\. Триангуляция высоты

# Высота объекта определяется с помощью тригонометрии. Зная расстояние от каждой станции до вычисленной точки на земле и угол возвышения (Elevation), алгоритм вычисляет предполагаемую высоту для каждого луча. Итоговая высота объекта формируется как среднее арифметическое значений, полученных от всех трех станций, что повышает точность и минимизирует влияние погрешности ввода.

# 🛠 Технический стек

# Проект построен на базе экосистемы .NET с использованием современных практик разработки десктопных приложений.

# Язык: C# 10.0+

# Фреймворк: .NET 6 / .NET 7 / .NET 8 (полная совместимость).

# UI Технология: Windows Presentation Foundation (WPF).

# Разметка: XAML с использованием стилей и ресурсов.

# IDE: Visual Studio 2022 или JetBrains Rider.

# Особенности реализации:

# Использование легковесных структур (Struct) для передачи данных, что снижает нагрузку на сборщик мусора (GC).

# Прямая работа с графическим примитивами внутри Canvas для обеспечения мгновенного отклика при изменении параметров.

# Отсутствие тяжелых сторонних зависимостей (Pure .NET), что делает приложение компактным и переносимым.

# 📥 Установка и запуск

# Чтобы запустить проект локально, выполните следующие шаги:

# Клонирование репозитория:

# code

# Bash

# git clone https://github.com/your-username/triangulator.git

# Открытие проекта:

# Запустите файл решения Triangulator.sln в Visual Studio 2022.

# Сборка:

# Убедитесь, что у вас установлена рабочая нагрузка ".NET Desktop Development". Нажмите Ctrl + Shift + B для сборки решения.

# Запуск:

# Нажмите F5 для запуска приложения в режиме отладки.

# 🎮 Руководство пользователя

# Интерфейс разделен на две основные функциональные зоны для удобства работы.

# Панель управления (Слева)

# Side Length (L): Задает базовое расстояние между станциями (масштаб всей системы). Поддерживает диапазон от 0.5м до 25м с возможностью дробного ввода.

# Station Controls (S1, S2, S3):

# Up-Down (Elev): Угол возвышения "лазера". Влияет на расчет высоты (Object Height). Чем больше угол, тем выше предполагаемый объект при том же удалении.

# Left-Right (Azim): Угол поворота "лазера". Влияет на координаты X/Y на карте. Значение 0 автоматически направляет луч в центр треугольника, что значительно ускоряет первоначальную настройку.

# Совет: Вы можете вводить точные значения вручную в текстовые поля. Система поддерживает мгновенный ввод: как только вы напечатаете цифру, карта сразу же обновится, нажатие Enter не требуется.

# Дашборд (Справа)

# Верхняя панель: Отображает итоговые рассчитанные метрики.

# Зеленый статус LOCKED: Система успешно вычислила точку пересечения.

# Красный статус NO FIX: Лучи параллельны, расходятся в разные стороны или направлены некорректно.

# Карта: Визуализирует положение станций (зеленые точки), направление лучей (пунктирные синие линии) и предполагаемую цель (красная точка с эффектом свечения). Рядом с целью отображается динамическая метка с текущей высотой объекта.

# 📂 Структура кода

# Проект организован в соответствии с принципами чистого кода, разделяя логику представления и вычислительное ядро.

# code

# Text

# Triangulator/

# ├── MainWindow.xaml       # Верстка интерфейса (Grid, Borders, Styles, Triggers)

# ├── MainWindow.xaml.cs    # Code-behind: Связывание UI, логика рисования на Canvas

# ├── TriangulationMath.cs  # Статический класс с чистой геометрической логикой

# ├── App.xaml              # Точка входа приложения и глобальные ресурсы

# └── ...

# MainWindow.xaml: Содержит определения стилей (CardStyle, InputBoxStyle), шаблоны элементов управления и общую структуру Dashboard.

# TriangulationMath.cs: Изолированный класс, содержащий метод CalculatePosition. Он принимает сырые данные от слайдеров и возвращает типизированный результат. Этот код полностью отвязан от пользовательского интерфейса.

# <p align="center">

# <i>Разработано с вниманием к деталям и любовью к геометрии.</i>

# <br>

# <sub>v2.1.4 Stable Build</sub>

# </p>

